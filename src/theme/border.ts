import * as colors from './color';

export const regularWhite = `1px solid ${colors.white}`;
export const regularBlack = `1px solid ${colors.black}`;
export const boldWhite = `2px solid ${colors.white}`;
